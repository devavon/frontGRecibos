'use client'; 

import UserListDashboard from "../../../components/UserListDashboard"; 

import React from "react";

const SecurityTestsPage = () => {
    // Este componente ahora renderiza el dashboard que llama a la API real.
    return <UserListDashboard />;
};

export default SecurityTestsPage;